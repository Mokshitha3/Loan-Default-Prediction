# 📊 Loan Default Prediction App

This is a **Streamlit** web app that:
- Loads the **UCI Credit Card Default** dataset.
- Trains a **Logistic Regression** model.
- Evaluates model performance (Accuracy, Precision, Recall, F1, MSE, RMSE).
- Visualizes the Confusion Matrix and ROC Curve.
- Allows **new customer input** for live default prediction!

---

## 🚀 How to Run the App

### 1. Clone the Repository
```bash
git clone https://github.com/your-username/loan-default-prediction-app.git
cd loan-default-prediction-app
```

### 2.Install the requirements
```bash
pip install -r requirements.txt
```

### 3.Run the streamlit app
```bash
streamlit run main.py
```
